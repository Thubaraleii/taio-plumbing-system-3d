Os arquivos .obj gerados pelo script `03_exportar_obj_para_blender.py` vao
aparecer aqui (um por superficie/formacao). Este arquivo README so existe
para a pasta nao ficar vazia no controle de versao/backup.
